server:
  port: 8082

spring:
  application:
    name: smartcity-parking

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

urbanlens:
  telemetry:
    tick-ms: 10000

management:
  endpoints:
    web:
      exposure:
        include: health,info
